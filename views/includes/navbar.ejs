<nav class="sticky top-0 z-50 border-b border-slate-200/80 bg-white/85 backdrop-blur-xl">
  <div class="mx-auto flex max-w-7xl flex-wrap items-center justify-between gap-4 px-6 py-4 lg:px-8">
    <a href="/" class="flex min-w-0 items-center gap-3">
      <div class="flex h-11 w-11 shrink-0 items-center justify-center rounded-2xl bg-rose-500 text-lg text-white shadow-lg shadow-rose-200/70">
        <i class="fa-solid fa-compass"></i>
      </div>
      <div class="min-w-0">
        <p class="text-[11px] font-semibold uppercase tracking-[0.35em] text-slate-400">Travel portfolio</p>
        <p class="truncate text-lg font-extrabold tracking-tight text-slate-900">Wanderlust</p>
      </div>
    </a>

    <div class="order-3 flex w-full flex-wrap items-center justify-center gap-2 text-sm font-semibold text-slate-600 sm:order-2 sm:w-auto sm:justify-start">
      <a class="rounded-full px-4 py-2.5 transition hover:bg-slate-100 hover:text-slate-900" href="/">Home</a>
      <a class="rounded-full px-4 py-2.5 transition hover:bg-slate-100 hover:text-slate-900" href="/listings">Listings</a>
      <% if (currentUser) { %>
        <a class="rounded-full px-4 py-2.5 transition hover:bg-slate-100 hover:text-slate-900" href="/listings/new">Host a Stay</a>
      <% } %>
    </div>

    <div class="order-2 flex flex-wrap items-center justify-end gap-2 sm:order-3">
      <% if (currentUser) { %>
        <span class="inline-flex max-w-[13rem] items-center gap-2 rounded-full border border-slate-200 bg-slate-50 px-4 py-2.5 text-sm font-semibold text-slate-700">
          <i class="fa-regular fa-user text-slate-500"></i>
          <span class="truncate"><%= currentUser.username %></span>
        </span>
        <a
          href="/auth/logout"
          class="inline-flex items-center justify-center rounded-full border border-slate-300 px-5 py-2.5 text-sm font-semibold text-slate-700 transition hover:border-slate-900 hover:bg-slate-900 hover:text-white"
        >
          Logout
        </a>
      <% } else { %>
        <a
          href="/auth/login"
          class="inline-flex items-center justify-center rounded-full px-5 py-2.5 text-sm font-semibold text-slate-700 transition hover:bg-slate-100 hover:text-slate-900"
        >
          Login
        </a>
        <a
          href="/auth/signup"
          class="inline-flex items-center justify-center rounded-full border border-slate-300 px-5 py-2.5 text-sm font-semibold text-slate-700 transition hover:border-slate-900 hover:text-slate-900"
        >
          Signup
        </a>
      <% } %>
    </div>
  </div>
</nav>
